개인 지식베이스 월간 리포트 — 프로젝트 기획서

1. 프로젝트 개요
   프로젝트명: MyLLM Report (가칭)
   한 줄 정의: 본인이 LLM(Claude, ChatGPT 등)과 나눈 대화 데이터를 분석해, 월간 단위로 "무엇을 가장 많이 물어봤는지 · 어떤 주제에 시간을 썼는지 · 잘한 점과 개선점"을 자동으로 정리해주는 개인용 회고 도구.
   왜 이 프로젝트인가:
   LLM 활용 개발자로서 필요한 6개 역량 축(API 운영, 프롬프트 엔지니어링, 오케스트레이션, 평가 체계, 로컬 모델 서빙, 데이터 파이프라인)을 하나의 프로젝트로 실습 가능
   개인정보(본인 대화)를 다루는 프로젝트이므로 로컬 모델 서빙을 우선 적용하는 것이 자연스러운 설계 명분이 됨
   진행 원칙: 이 프로젝트는 기존에 운영 중인 다른 서비스(홈서버 인프라 등)와 완전히 분리해서 진행한다. 1단계는 순수 로컬 개발 환경에서만 완결하고, 서버 배포 여부·방식은 1단계가 끝난 뒤 별도로 결정한다.
2. 목표
   1단계 목표 (이번 기획의 범위 — 순수 로컬)
   로컬 개발 환경(본인 PC)에서 로컬 LLM(Ollama 기반)으로 대화 분류·요약·리포트 생성 파이프라인을 처음부터 끝까지 완성
   클라우드 API 없이 로컬 장비에서 LLM을 구동·관리한 경험을 포트폴리오화
   외부 서버, 기존 인프라와 무관하게 로컬에서 실행 가능한 상태로 완결
   2단계 (별도 기획, 1단계 완료 후 착수 여부 결정)
   완성된 파이프라인을 상시 접근 가능한 형태로 배포할지, 배포한다면 어떤 환경에 할지는 이때 별도로 검토
   클라우드 API 버전과 응답 품질·속도 비교 리포트
   익명 통계 기반 다인원 비교 기능
3. 범위 정의 (1단계 MVP — 순수 로컬)
   구분 포함 제외
   실행 환경 본인 로컬 PC(맥/윈도우 등 개발 머신) 외부 서버 배포 전반
   데이터 소스 Claude/ChatGPT 대화 export(JSON) 수동 업로드 실시간 API 연동, 자동 수집
   처리 방식 로컬 LLM(Ollama)으로 전량 처리 클라우드 API 처리(2단계로 이관)
   출력 월간 리포트(주제 Top N, 토큰 소비량, 잘한 점/개선점) 다인원 비교, 리더보드
   배포 없음 — CLI 실행 또는 로컬호스트 접속으로 확인 상시 구동, 외부 접근, 도메인 연결
4. 시스템 아키텍처 (1단계, 로컬 전용)
   [본인 로컬 PC]

[export.json 파일]
│ (1) CLI 명령으로 파일 경로 지정
▼
[파서 모듈] ── Claude/ChatGPT export 스키마 → 공통 스키마 정규화
│
▼
[로컬 LLM 호출 모듈] ── Ollama REST API(localhost:11434)
① 대화별 주제 분류
② 대화별 요약
③ 월간 종합 리포트 생성(잘한 점/개선점)
│
▼
[검증 모듈] ── 분류 정확도 샘플 체크(LLM-as-judge)
│
▼
[결과 저장] ── 로컬 SQLite 또는 JSON 파일 (신규 DB 인스턴스 불필요)
│
▼
[리포트 출력] ── CLI 출력 또는 로컬 정적 HTML 파일로 결과 확인
1단계에서는 별도 백엔드 서버 프로세스, Nginx, PM2, 외부 노출 터널 어느 것도 필요 없다. Node.js 스크립트(또는 CLI 도구)를 로컬에서 직접 실행하는 구조로 충분하다. 5. 기술 스택 (1단계, 최소 구성)
실행 환경: 본인 로컬 개발 머신, Node.js 스크립트 또는 CLI 형태
로컬 LLM 서빙: Ollama (Llama 3.1 8B 또는 Qwen2.5 7B급 모델부터 시작, 본인 PC 사양에 맞춰 조정)
언어: TypeScript/Node.js
저장소: SQLite 또는 단순 JSON 파일 (외부 DB 불필요)
파싱: Claude/ChatGPT export JSON 스키마 분석 후 정규화 스크립트
결과 확인: CLI 출력 우선, 필요 시 간단한 로컬 정적 HTML로 시각화

5-1. 코드 품질 원칙
- 플랫폼 식별자('claude', 'chatgpt')·역할 문자열·Ollama 기본값 등 반복 사용되는 리터럴은
  src/constants.ts 에서 상수로 일괄 관리한다.
- 외부 경계(export JSON 파싱, Ollama API 응답, LLM JSON 출력)에는 Zod 스키마(src/schemas.ts)를
  적용해 런타임 타입 안전성을 확보한다. 내부 로직 간 전달은 TypeScript 타입만으로 충분하다.
- 새로운 외부 데이터 소스나 LLM 출력 포맷이 추가될 때마다 schemas.ts 에 스키마를 먼저 정의한 뒤
  구현 코드를 작성한다 (Schema-First 원칙).

5-3. 비교 기준 전략 (자기객관화 지원)
1단계 (로컬): 이전 달 행동 지표와 자동 비교. 예: "지난 달 꼬리질문 비율 45% → 이번 달 73%, +28%p 향상"
2단계 (상용화):
  - 사용자 동의 하에 행동 지표(conversation_id·내용 제외)를 익명 집계 서버에 전송
  - 집계 서버는 지표 분포(평균, 중앙값, 백분위)만 저장 — 원본 대화 비공개
  - 리포트에 백분위 표시: "꼬리질문 비율 73% — 전체 사용자 상위 18%"
  - 구현 시 추가할 것: POST /api/metrics (익명 지표), GET /api/benchmarks (집계 통계)

5-2. 확장성 전략 (로컬 → 상용화)
- LLM 호출은 ILLMClient 인터페이스를 통해서만 이루어지며, LLM_PROVIDER 환경변수 하나로
  구현체를 교체한다 (현재: ollama, 2단계: claude | openai 추가 예정).
- DB 접근은 IStorage 인터페이스를 통해서만 이루어지며, getStorage() 팩토리를 통해 단일
  인스턴스를 공유한다. 2단계에서 SQLite → Postgres 전환 시 Storage 구현체만 교체한다.
- 새 LLM 제공자나 DB 백엔드 추가 시: (1) 구현 클래스 작성 → (2) factory switch 케이스 추가
  → (3) 환경변수 값 문서화. index.ts 등 상위 코드는 수정 불필요.

6. 데이터 흐름 상세
업로드(로컬): Claude/ChatGPT 설정에서 내려받은 export JSON을 로컬 폴더에 저장
파싱·정규화: 두 플랫폼의 서로 다른 JSON 구조를 {date, role, content, char_count} 공통 스키마로 변환
1차 LLM 처리 (분류): 대화 단위로 로컬 LLM에 "주제 태그 + 예상 토큰 수" 추출 요청 → JSON 강제 출력
2차 LLM 처리 (요약): 주제별로 묶은 대화들을 요약
3차 LLM 처리 (종합 리포트): 한 달 전체 요약을 입력으로 "이번 달 가장 많이 다룬 주제 Top 5", "잘한 점", "아쉬운 점"을 생성
검증: 무작위 샘플 5~10건을 사람이 직접 확인해 분류 정확도 기록
저장·확인: 결과를 로컬 SQLite/JSON에 저장하고 CLI 또는 로컬 HTML로 확인 7. 로컬 LLM CLI로 개발을 진행하는 방식
이 프로젝트 자체를 로컬 LLM 기반 CLI 코딩 도구(예: Ollama 모델을 붙인 aider, Continue CLI 등)에게 지시해서 개발하려는 경우, 아래 순서를 따르면 작업이 훨씬 안정적으로 진행된다.

1. 저장소와 스펙 문서부터 준비
   이 기획서를 프로젝트 루트에 SPEC.md 형태로 넣어두고, CLI 도구에게 "이 문서를 컨텍스트로 참고해서 작업해줘"라고 먼저 인지시킨다. 로컬 모델은 클라우드 모델보다 컨텍스트 유지력이 약한 경우가 많아서, 매 세션 시작 시 스펙 문서를 다시 읽게 하는 습관이 필요하다.
2. 한 번에 하나의 작은 단위만 지시
   로컬 모델(특히 7B~8B급)은 한 번에 여러 파일을 아우르는 큰 작업 지시에 약하다. "6번 데이터 흐름의 2단계(파서 모듈)만 구현해줘" 식으로, 섹션 4의 아키텍처 다이어그램에 있는 박스 하나 단위로 쪼개서 지시하는 게 안전하다.
   순서 예시: ① export 파서 → ② Ollama 호출 래퍼 함수 → ③ 분류 프롬프트 → ④ 요약 프롬프트 → ⑤ 종합 리포트 프롬프트 → ⑥ 검증 스크립트 → ⑦ 저장 로직
3. 각 지시마다 완료 기준(acceptance criteria)을 명시
   "이 함수는 export.json 경로를 받아서 {date, role, content, char_count}[] 배열을 리턴해야 해. 테스트로 샘플 파일 하나 넣어서 콘솔에 찍어봐" 처럼, 입력·출력·검증 방법까지 한 문장에 같이 지시한다. 모호하게 지시하면 로컬 모델일수록 엉뚱한 방향으로 구현할 확률이 높다.
4. 결과를 매번 직접 실행해서 확인
   CLI가 코드를 생성한 뒤 바로 다음 지시로 넘어가지 말고, 실제로 node script.js 등으로 실행해 본 뒤 다음 단계로 진행한다. 로컬 모델은 실행하지 않은 코드에 오류가 섞여 있는 빈도가 클라우드 모델보다 높다.
5. 실패 시 되돌리기 쉽게 커밋 단위를 잘게
   위 7단계 각각을 커밋 단위로 나눠서, 특정 단계에서 로컬 모델이 잘못된 방향으로 구현하면 그 커밋만 되돌리고 다시 지시할 수 있게 한다.
6. 로컬 모델이 막히면 지시를 더 쪼개거나 예시를 붙인다
   같은 지시를 반복해도 안 되면, 원하는 입출력 예시(샘플 JSON 한 줄)를 프롬프트에 직접 넣어준다. 로컬 모델일수록 구체적 예시 하나가 추상적 설명 여러 줄보다 효과적이다.

8) 개발 일정 (1단계, 순수 로컬 기준)
   주차 작업 비고
   1주차 Ollama 로컬 설치, 모델 다운로드, export 파서 개발 CLI 도구에게 파서부터 작은 단위로 지시
   2주차 분류·요약 프롬프트 개발 및 테스트 각 프롬프트를 별도 함수/파일로 분리
   3주차 월간 종합 리포트 생성 + 검증 모듈 구현 샘플 데이터로 분류 정확도 측정
   4주차 저장 로직(SQLite/JSON) + 결과 확인용 로컬 출력 완성 여기까지가 1단계 완료 시점
   5주차 실제 본인 데이터로 1개월 리포트 생성, 결과 정리 배포 여부는 이후 별도 논의
9) 성공 기준 (1단계)
   로컬 PC에서만 실행해도 분류·요약·리포트 생성까지 전체 파이프라인이 동작
   외부 서버, 별도 인프라 없이 완전히 로컬로 완결
   본인 대화 데이터 기준 분류 정확도 검증 결과를 수치로 제시 가능(예: 샘플 정확도 OO%)
   로컬 LLM CLI 도구로 작업을 지시해서 실제 코드가 완성된 과정 자체를 기록(어떤 지시가 잘 먹혔는지, 어디서 막혔는지)
10) 리스크 및 대응
    리스크 대응 방안
    로컬 PC 사양이 LLM 구동에 부족할 수 있음 경량 모델(7B급)부터 시작, 필요 시 양자화(quantized) 모델 사용
    로컬 모델 분류 정확도가 낮을 수 있음 검증 모듈로 정확도 측정 후, 필요 시 프롬프트 개선 또는 클라우드 API 병행(2단계)
    로컬 LLM CLI가 지시를 잘못 이해해 엉뚱하게 구현 작업 단위를 잘게 쪼개고, 커밋 단위로 나눠 되돌리기 쉽게 진행
    개인정보(대화 내용) 노출 원본 대화는 처리 후 즉시 삭제 또는 로컬 보관, 외부 전송 없음
11) 향후 확장 아이디어 (2단계 이후, 별도 논의)
    1단계 완료 후, 배포가 필요하다고 판단되면 그때 어떤 환경(신규 서버든 기존 서버든)에 올릴지 별도로 검토
    클라우드 API(Claude/GPT) 버전과 A/B 비교 리포트 → 5번 축을 넘어 1번(API 운영) 축까지 확장
    익명 통계 기반 "다른 사람과 비교" 기능 (원본 대화 공유 없이 통계값만 집계)
    Slack/Notion 연동으로 월간 리포트 자동 발송 (6번 축 강화)
